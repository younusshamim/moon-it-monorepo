// @moonit/auth — Better Auth config, the role enum (admin / staff / instructor), and guards
// shared by the NestJS guard and the Next.js middleware so the two never disagree.
// See INFRASTRUCTURE.md §9. Implementation lands in a later session.
export {};
