// @moonit/ai — transport-agnostic RAG + chat core (ingest / retrieve / chat), shared by the web
// streaming widget and the Messenger webhook. Calls domain services through injected interfaces;
// never imports the API's modules directly. Vectors via @moonit/db. See INFRASTRUCTURE.md §6.
// Implementation lands in a later session.
export {};
