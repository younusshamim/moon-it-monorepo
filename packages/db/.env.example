# Connection string for drizzle-kit (migrate/push/studio) and the seed runner.
# Requires a pgvector-capable Postgres (e.g. the pgvector/pgvector image). See tooling/docker.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/moonit
